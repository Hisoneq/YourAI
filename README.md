# Glass Morphism Background

Современный React компонент с эффектом glass morphism и интерактивным размытым кружком, следующим за курсором.

## 🎨 Особенности

- ✨ **Glass Morphism эффект** - красивое размытое стеклянное покрытие с backdrop-filter
- 🎯 **Интерактивный кружок** - размытый градиентный круг, который плавно следует за курсором с задержкой
- 🎨 **Многослойность** - кружок находится "за стеклом", создавая глубину
- ⚙️ **Гибкая настройка** - легко изменять цвета, размытие, размеры и скорость анимации
- 🎭 **Варианты цветов** - 4 предустановленных цветовых схемы
- 🔧 **TypeScript** - полная типизация
- 🎪 **Context API** - удобное управление состоянием фона через контекст

## 🚀 Запуск проекта

```bash
npm install
npm run dev
```

## 📁 Архитектура проекта

```
src/
├── components/
│   ├── BackgroundProvider/       # Context provider для управления фоном
│   │   ├── index.tsx            # React компонент Provider
│   │   └── context.ts           # Context и типы
│   └── GlassBackground/          # Основной компонент glass эффекта
│       ├── GlassBackground.tsx   # Главный компонент
│       ├── GlassBackground.module.css  # Стили
│       ├── types.ts             # TypeScript типы и интерфейсы
│       └── index.ts             # Экспорты
├── hooks/
│   └── useBackground.ts         # Хук для доступа к контексту фона
└── App.tsx                      # Демонстрация использования
```

### Структура компонентов

**GlassBackground** - главный компонент, состоящий из слоев:
1. `cursorFollower` - размытый кружок (z-index: 1)
2. `glass` - стеклянный слой с backdrop-filter (z-index: 2)
3. `noise` - текстурный шум (z-index: 3)
4. `content` - пользовательский контент (z-index: 10)

**BackgroundProvider** - Context Provider для:
- Централизованного управления настройками фона
- Динамического изменения параметров из любой части приложения
- Сохранения состояния

## 💡 Использование

### Базовое использование

```tsx
import { BackgroundProvider } from './components/BackgroundProvider';

function App() {
  return (
    <BackgroundProvider>
      <YourContent />
    </BackgroundProvider>
  );
}
```

### С настройками

```tsx
<BackgroundProvider
  initialProps={{
    variant: 'purple',           // 'default' | 'purple' | 'blue' | 'gradient'
    blurIntensity: 15,           // 0-30 - размытие glass слоя
    opacity: 0.05,               // 0-1 - прозрачность glass
    interactive: true,            // включить/выключить следование за курсором
    animationSpeed: 'normal',    // 'slow' | 'normal' | 'fast'
    cursorFollowerSize: 300,     // размер кружка в px
    cursorFollowerBlur: 80       // размытие кружка в px
  }}
>
  <YourContent />
</BackgroundProvider>
```

### Динамическое изменение настроек

```tsx
import { useBackground } from './hooks/useBackground';

function YourComponent() {
  const { setBackgroundProps, currentProps } = useBackground();
  
  return (
    <button onClick={() => setBackgroundProps({ variant: 'purple' })}>
      Изменить цвет
    </button>
  );
}
```

## 🎨 Цветовые варианты

- **default** - фиолетово-синий градиент
- **purple** - насыщенный фиолетовый
- **blue** - яркий синий
- **gradient** - розово-фиолетово-синий переход

## 🛠 Технологии

- React 18
- TypeScript
- CSS Modules
- Vite
- Context API

## 🎯 Как это работает

### Эффект следования за курсором

Компонент использует:
1. `requestAnimationFrame` для плавной анимации
2. Линейную интерполяцию для создания эффекта задержки (lag effect)
3. `transform: translate()` для производительного позиционирования
4. `will-change: transform` для оптимизации рендеринга

### Glass Morphism

Эффект достигается через:
- `backdrop-filter: blur()` - размытие фона
- `background: rgba()` - полупрозрачный фон
- `border` - тонкая светлая граница
- Многослойная структура для создания глубины

## 📝 Лицензия

MIT
